# sift audit — Tweaks Forever @ 4516559

Scope: whole repo (second run of the day, after the setup PR #47). Gate before: pass. Gate after: pass.
Reviewer: primary, 9 envelopes (`.sift/runs/`, not committed). All were validated at 4516559. The four
whose files this audit changed now report INCOMPLETE ("file changed since review"), which is expected.

## Carried forward

| Item from 2026-09-23.md | Disposition |
|---|---|
| `Automation.lua` resurrection from an ungrouped player in combat | fixed in 07ceb48 (`UnitExists(offerer)` check) |
| `Errors.lua:12`, `Vendor.lua:10`: hand-written Leatrix Plus conflicts | still present, still unproven (below) |
| `Junk.lua:26`, `:360`: tooltips hard-code 12 | fixed: built from `BATCH_SIZE`, and the text is byte-identical |
| `Frames.lua`, `Gear.lua` oversized | still present; a split belongs in its own reviewed change |

## Slices

| Slice | Files read | Reported | Proven | Changed | Unproven | Suppressed | Gate |
|---|---|---|---|---|---|---|---|
| `tools/` | 13/13 | 1 | 1 | 0 | 1 | 0 | pass |
| docs (CHANGELOG is history; head only) | 4/4 | 3 | 3 | 3 | 0 | 0 | pass |
| config (incl. `types/`) | 15/15 | 2 | 2 | 2 | 0 | 0 | pass |
| `tests/` | 13/13 | 0 | 0 | 0 | 0 | 0 | pass |
| core | 5/5 | 1 | 0 | 0 | 1 | 0 | pass |
| features | 10/10 | 1 | 1 | 1 | 0 | 0 | pass |
| ui (Tooltips, Nameplates, Sections, Reagents) | 4/4 | 0 | 0 | 0 | 0 | 0 | pass |
| bags | 2/2 | 3 | 1 | 1 | 2 | 0 | pass |
| `Frames.lua` | 1/1 | 1 | 0 | 0 | 1 | 0 | pass |

## Changes

| Slice | Change | Category | Lines |
|---|---|---|---|
| config | `DebuffFrame` and `GameTooltip_AddBlankLineToTooltip` read_globals, both unused since 664c196 | dead-code | −2 |
| docs | Store page said nameplates draw the target in gold; they draw it in white and the focus in gold | stale-docs | 0 |
| docs | `tools/README.md` never listed `gen_zonelevels.py`; the README dev block left out `gen_dungeons.py` | stale-docs | +10 |
| features | `Campsites.lua`: `TENT` constant used in FEATURES; `CAMPFIRES` derived from the Camp Benefits rows | parallel-implementations | +5 |
| bags | `Junk.lua`: both batch tooltips built from `BATCH_SIZE`, constant moved above the features | parallel-implementations | +2 |

Net: +15 lines (+26/−11) across 6 files: docs +10, Lua +7, `.luacheckrc` −2. Preservation: the CAMPFIRES set is the same three
spells, and a stubbed load of `Junk.lua` returns the `sellJunk` tooltip byte-identical.

## Bugs found, not fixed

None.

## Unproven candidates

| Location | Candidate | What would settle it |
|---|---|---|
| `tools/gen_zonelevels.py:96` + `gen_dungeons.py:77`, `gen_overlays.py:39`, `gen_camp.py:33` | `db2()` fetch and cache copied into every generator, plus a pinned `BUILD` in each. The copies differ in timeouts, schema checks and CLI (gen_camp's `--refresh`/`--offline` are not mutually exclusive). | A shared `tools/wago.py`, after PR #45 merges, since its `gen_classspells.py` adds a fifth copy. Unifying changes CLI and timeouts, so it needs its own review. |
| `Errors.lua:12`, `Vendor.lua:10` | Leatrix Plus conflicts repeat `Automation.lua:7` `Leatrix(key)` | A Core helper means `errors_spec` must load Core.lua (as `core_spec` does) rather than stub `ns`. |
| `Gear.lua:378` | `Coloured` hand-builds `\|cff…`; `CreateColor(r, g, b):WrapTextInColorCode` exists on this client (QuestDistance uses `CreateColor`) | Blizzard rounds each byte where this floors, so colours may shift by one step. Check in game whether that matters. |
| `Frames.lua:1`, `Gear.lua:1` | Oversized (871 and 735 lines) | A split in its own reviewed change. |

Overlaps with open PRs, from `git merge-tree` trial merges:

- #45 touches `.luacheckrc`, `README.md` and `tools/README.md`. All three auto-merge with this branch. Its one
  conflict, in `refresh-data.yml`, is already there against main.
- #46 edits the Maps bullet in `docs/curseforge.md`, which sits next to the corrected Interface bullet, so
  the two conflict. The resolution is to keep both lines. The fix goes ahead anyway because it corrects a
  wrong fact on the store page.

## Learning captured

- Rules added: none. Unused read_globals need a cross-file search, not a pattern.
- Lenses added: none.
- Project skill updates:
  - An evidence row for unused `.luacheckrc` globals.
  - Live roots for the modules added since the last audit (Sections, Reagents, Camp, ZoneLevels, Entrances,
    FutureSpells, ClickMode, the Data tables, the pin mixin, `trainer`).
  - `refresh-data.yml` as the list of generators.
  - Zones for `types/`, `docs/curseforge.md`, `docs/screenshots/` and `.agents/`/`.sift/`.
  - A seven-step risk order.
  - Dismissed candidates.

## Next

After #45 merges, unify the five `db2()` copies into one `tools/` module.
