# sift audit — Tweaks Forever @ d9e01cf

Scope: whole repo. Gate before: pass (after fixing a setup regression, e749b42). Gate after: pass.
Reviewers: primary (all slices, 73 envelopes) plus an independent second pass on `bags` and `frames`
(26 envelopes, `.sift/runs/second/`). Every envelope validated CLEAN or FINDINGS at the reviewed HEAD.

## Carried forward

None: first audit.

## Slices

| Slice | Files read | Reported | Proven | Changed | Unproven | Dismissed | Gate |
|---|---|---|---|---|---|---|---|
| `tools/` | 2/2 | 0 | 0 | 0 | 0 | 0 | pass |
| docs (`README.md`, `CHANGELOG.md`) | 2/2 | 1 | 1 | 1 | 0 | 0 | pass |
| config (`.github`, `.pkgmeta`, lint/format configs, `.toc`) | 10/10 | 1 | 1 | 1 | 0 | 0 | pass |
| `tests/` | 6/6 | 0 | 0 | 0 | 0 | 0 | pass |
| core (`Core`, `Reload`, `Errors`, `Vendor`, `Settings`) | 5/5 | 3 | 3 | 1 | 1 | 0 | pass |
| features (`Automation`, `Exploration`, `Fishing`) | 3/3 | 1 | 1 (bug) | 0 | 0 | 0 | pass |
| bags (`Junk`, `Gear`) | 2/2 | 6 | 4 | 3 | 1 | 2 | pass |
| `Frames.lua` | 1/1 | 2 | 1 | 0 | 0 | 1 | pass |

## Changes

| Slice | Change | Category | Lines |
|---|---|---|---|
| docs | README said CI runs lint and specs; it runs seven checks | stale-docs | 0 |
| config | `CreateSettingsListSectionHeaderInitializer` read_global, unused since 53c8394 | dead-code | −1 |
| core | Feature-shape comment omitted `parent` | stale-docs | +1 |
| bags | Bag-button hooks + remapped-click guard unified into `ns.HookBagButtons` / `ns.IsBagActionClick`; Junk's two refund checks into `Refundable` | parallel-implementations | −7 |

Net: −7 lines across 5 files (Lua −6). Preservation for the bags change: a stubbed load of Core+Junk+Gear
records identical hook registrations and click-guard calls before and after.

## Bugs found, not fixed

| Location | Evidence | Suggested fix |
|---|---|---|
| `Automation.lua:188-189` | `RESURRECT_REQUEST` passes the offerer's **name**; `UnitAffectingCombat(name)` resolves names only for group members, so a resurrection from an ungrouped player in combat is auto-accepted, contrary to the tooltip ("Not from someone in combat"). Not verified in the client. | Treat an unresolvable offerer as "unknown" (skip auto-accept) or check `UnitExists(offerer)` first. |

## Unproven candidates

| Location | Candidate | What would settle it |
|---|---|---|
| `Errors.lua:12`, `Vendor.lua:10` | Hand-written Leatrix Plus conflicts duplicate `Automation.lua:6` `Leatrix(key)` | Moving it to Core means every spec's `ns` stub must re-implement it (`errors_spec` reads the conflict); worth it only if a spec loads Core. |
| `Junk.lua:24`, `:330` | Tooltips hard-code 12, restating `BATCH_SIZE` | Player-facing strings: a proposal to build them from `BATCH_SIZE` (needs the constant above the feature declarations). |
| `Frames.lua:1`, `Gear.lua:288` | Oversized: Frames mixes Model / apply hooks / Edit Mode UI / lifecycle; Gear's Init mixes marks, menu, tooltip, fishing tracking | A split in its own reviewed change; audits do not split. |

Dismissed (second reviewer): stringly-typed `Frames.lua:51` (persisted `windowLayouts` key format) and
`Gear.lua:30` (`gearMark` set fails loudly on unknown values).

## Learning captured

- Rules added: none (no remaining repeated shape a tool could match without judgment).
- Lenses added: none.
- Project skill: new live roots `ns.HookBagButtons`/`ns.IsBagActionClick` and how to prove changes to them;
  small-clone jscpd run; dismissed candidates.

## Next

Reproduce the resurrection bug in the client (ungrouped resurrecter in combat) and fix it.
