# sift audit — Tweaks Forever @ 4955419

Scope: whole repo. Gate before: pass. Gate after: pass (every step, after each slice).
Reviewers: tools and config by sub-agents, the rest by the primary; 10 envelopes in `.sift/runs/` (not
committed), all validated FINDINGS against 4955419 (21 reported, 0 unverifiable). A second model per slice was
not possible: the shared sub-agent limit was full.

## Carried forward

| Item from 2026-09-23-2.md | Disposition |
|---|---|
| `tools/gen_*.py` `db2()` copies | still present; a shared `tools/wago.py` is a new module, and PR #45 adds a fifth generator (`gen_classspells.py`) that should land on it |
| `Errors.lua:14`, `Vendor.lua:12`: hand-written Leatrix Plus conflicts | still present, still unproven (below) |
| `Gear.lua:378` `Coloured` vs `CreateColor` | still present, still unproven (below) |
| `Frames.lua`, `Gear.lua` oversized | still present; a split belongs in its own reviewed change |
| Open PRs #45, #46 | both open; both already conflict with main (refresh-data.yml, docs/curseforge.md). This branch adds no new conflict with either (`git merge-tree`) |

## Slices

| Slice | Files read | Reported | Proven | Changed | Unproven | Suppressed | Gate |
|---|---|---|---|---|---|---|---|
| `tools/` | 13/13 | 5 | 4 | 0 | 1 | 0 | pass |
| docs (incl. the profile) | 6/6 | 1 | 1 | 1 | 0 | 0 | pass |
| config (incl. `types/`, `.pkgmeta`) | 15/15 | 3 | 3 | 3 | 0 | 0 | pass |
| `tests/` | 13/13 | 1 | 1 | 1 | 0 | 0 | pass |
| core | 5/5 | 1 | 0 | 0 | 1 | 0 | pass |
| features | 10/10 | 2 | 2 | 0 | 0 | 0 | pass |
| ui | 4/4 | 1 | 1 | 1 | 0 | 0 | pass |
| bags | 2/2 | 3 | 2 | 1 | 1 | 0 | pass |
| `Frames.lua` | 1/1 | 1 | 1 | 0 | 0 | 0 | pass |
| standards (`wow-forever-addon`) | 14/14 | 3 | 3 | 0 | 0 | 0 | pass |

## Changes

| Slice | Change | Category | Lines |
|---|---|---|---|
| tests | `spellbook_spec.lua`: setfenv env stubbing `GREEN_FONT_COLOR:WrapTextInColorCode`, uncalled since 664c196 | dead-code | −7 |
| config | `C_NamePlate`, `NamePlateDriverFrame`, `NamePlateSetupOptions` moved from writable `globals` to `read_globals`; nothing writes them | dead-code | 0 |
| config | `types/Forever.lua`: `NamePlateHealthBar.IsTarget`, no reader since bd5afa2 | dead-code | −1 |
| config | `.pkgmeta`: three dot-file `ignore:` entries the pinned packager prunes anyway (release.sh:1828 at v2.6.1) | dead-code | −3 |
| bags | `Gear.lua`: the `marks` shape comment moved from `hooked` onto `marks` | comment-narration | 0 |
| ui | `Nameplates.lua`: status-icon hook passes `Refresh` instead of an inline copy of it | parallel-implementations | −4 |
| docs | README "Works alongside" intro now names Mapster among the conditional conflicts | stale-docs | 0 |

Net: −15 lines (+7/−22) across 7 files.

## Standards (proposals only)

| Requirement | Location | Gap |
|---|---|---|
| WFA-3 | `Settings.lua:85` | only `/tweaks`; the full-name `/tweaksforever` is missing |
| WFA-10 | `README.md` | ~1,940 words against 1,200; no Usage section (slash commands sit in Features, :81); Works alongside precedes Install |
| WFA-14 | `Gear.lua:718`, `Spellbook.lua:426` | `TweaksForeverCharDB` defaults are spread over read sites; no one place declares them |

## Left alone

| Location | Finding | Why |
|---|---|---|
| `tools/gen_dungeons.py:77` (+ camp, overlays, zonelevels) | four drifted `db2()` fetch/cache copies; `refresh-data.yml:28` checks `BUILD` in `gen_overlays.py` only | needs a new shared module; do it with or after PR #45 |
| `tools/lint_multivalue.py:13` | `Token.kind` closed set typed as `str` | low value, type-only |
| `Spellbook.lua:426`, `Gear.lua:718` | two `TweaksForeverCharDB = TweaksForeverCharDB or {}` inits; Core's login handler is the home | PR #45 rewrites Spellbook.lua |
| `Spellbook.lua:282` | column width recomputes `Model.Layout`'s formula (:84) | PR #45 rewrites Spellbook.lua |
| `Frames.lua`, `Gear.lua` | oversized | design change |

## Bugs found, not fixed

None.

## Unproven candidates

| Location | Candidate | What would settle it |
|---|---|---|
| `Errors.lua:14`, `Vendor.lua:12` | reuse `Automation.lua:7` `Leatrix(key)` via a Core export | agree on a Core API; conflict entries are only checked in game |
| `Gear.lua:378` | `CreateColor(...):WrapTextInColorCode` | show `math.floor(x*255)` and the engine's rounding give the same hex for every mark colour |
| `tools/gen_overlays.py:130` | char-by-char chunk loop → slicing | regenerate `Data/Overlays.lua` from a warm cache and diff byte-for-byte |

## Learning captured

- Rules added: none.
- Lenses added: none.
- Project skill updates: evidence rows for writable-but-never-written globals, dead `---@field`s, and the
  standards pack (`SIFT_STANDARDS_PATH=/home/cjber/skills`); the `.pkgmeta` dot-file claim now cites the
  packager source; four dismissed candidates.

## Next

A shared `tools/wago.py` for the generators, landed with or right after PR #45 so `gen_classspells.py` starts
on it and the refresh workflow checks `BUILD` in one place.
