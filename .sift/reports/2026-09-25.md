# sift audit — Tweaks Forever @ 8c4eed0 (pre-release, v0.3.0..main)

Scope: diff since the 2026-09-24 audit (1dd75ae..8c4eed0: #55, #56, #57) plus its carried items. Gate before:
pass. Gate after: pass (gitleaks left to CI). One envelope (`.sift/runs/post-audit.json`, not committed),
validated FINDINGS: 6/6 files read, 1 reported.

## Carried forward

| Item from 2026-09-24.md | Disposition |
|---|---|
| `tools/gen_*.py` `db2()` copies (now five) | still present; open data PR #58 edits every generator's `BUILD`, and a shared module needs a byte-for-byte regeneration diff |
| `Spellbook.lua` / `Gear.lua` `TweaksForeverCharDB` inits | fixed: Core's login handler creates it once |
| `Spellbook.lua` column width restating `Model.Layout` | fixed: one `ColumnWidth(grid)` |
| `Errors.lua`, `Vendor.lua` hand-written Leatrix conflicts | still present, still unproven |
| `Gear.lua` `Coloured` vs `CreateColor` | still present, still unproven |
| `tools/gen_overlays.py` chunk loop | still present, still unproven |
| WFA-3 (`/tweaksforever` alias) | still open; a new slash command is a feature, not an audit edit |
| WFA-10 (README length/order) | README now ~1,370 words with a Usage section; Works alongside still precedes Install |
| WFA-14 (`TweaksForeverCharDB` defaults) | the table is now created in one place; per-field defaults stay at their features |
| `Frames.lua`, `Gear.lua` oversized | still present |

## Changes

| Change | Category |
|---|---|
| `Core.lua`: creates `TweaksForeverCharDB` at login; `Gear.lua`, `Spellbook.lua` drop their copies | parallel-implementations |
| `Spellbook.lua`: `ColumnWidth(grid)` shared by `Model.Layout` and `Render` | parallel-implementations |
| `types/Namespace.lua`: `TFCamp` declares `Effect` and `Announcement` | stale-docs |

## Bugs found, not fixed

None.

## Next

A shared `tools/wago.py` for the five generators, after #58 merges.
